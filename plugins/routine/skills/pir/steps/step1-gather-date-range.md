# Step 1: Prepare

If `QUERY` is empty or missing, use `AskUserQuestion` to ask what to investigate (e.g. "incidents last 24h", "errors yesterday"). Do NOT ask about services, incident IDs, or severity — these are all derived from data.

Otherwise proceed directly to Step 2.
