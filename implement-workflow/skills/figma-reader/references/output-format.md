# Output Format

Present the design context extracted from Figma MCP.

Key information to capture:
- Component structure and hierarchy
- Layout details (spacing, alignment, sizing)
- Color values and typography
- Interactive states (hover, active, disabled)
- Responsive breakpoints if present

If the user skipped Figma scanning, return:
```
No Figma design context provided — skipped by user.
```
