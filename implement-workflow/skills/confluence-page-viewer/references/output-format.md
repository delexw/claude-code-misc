# Output Format

Present the raw `confluence read` output directly to the user.

If the output is too verbose, summarize the key sections:

```
Page: {title}
Space: {space}

{first few paragraphs or key content sections}
```
